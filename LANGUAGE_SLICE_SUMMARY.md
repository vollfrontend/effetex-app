# Створено Language Slice для управління мовами

## Що було створено

### 1. Language Slice (`/src/state/slices/languageSlice.ts`)
- Зберігає всі доступні мови (Українська, English, Русский, Polski)
- Поточна мова за замовчуванням: **Українська** (uk)
- Функції для зміни мови та отримання об'єкта поточної мови

### 2. Типи (`/src/state/types.ts`)
Додано нові типи:
- `LanguageCode` - тип для кодів мов ('uk' | 'en' | 'ru' | 'pl')
- `Language` - інтерфейс для об'єкта мови (code, name, nativeName)
- `LanguageSlice` - інтерфейс для language slice

### 3. Інтеграція в Store (`/src/state/userStore.ts`)
- Додано `languageSlice` до головного store
- Тепер доступний через `useStore`

### 4. Оновлено SideMenu (`/src/components/SideMenu/index.tsx`)
- Використовує новий language slice
- Підтримує циклічне перемикання між усіма доступними мовами
- Відображає назву мови рідною мовою

### 5. Створено хук useLanguage (`/src/hooks/useLanguage.ts`)
Зручний хук для роботи з мовами, який надає:
- `currentLanguage` - код поточної мови
- `currentLanguageObject` - об'єкт поточної мови
- `availableLanguages` - всі доступні мови
- `setCurrentLanguage` - функція для зміни мови
- `cycleLanguage` - циклічне перемикання мов
- `isCurrentLanguage` - перевірка поточної мови

### 6. Компонент LanguageSelector (`/src/components/LanguageSelector/index.tsx`)
Готовий UI компонент для вибору мови:
- Відображає всі доступні мови
- Підсвічує поточну вибрану мову
- Адаптується до теми додатку

### 7. Документація (`/src/state/slices/LANGUAGE_SLICE_README.md`)
Повна документація з прикладами використання

## Як використовувати

### Швидкий старт з хуком:
```typescript
import { useLanguage } from '@/src/hooks/useLanguage';

const MyComponent = () => {
  const { currentLanguageObject, cycleLanguage } = useLanguage();

  return (
    <View>
      <Text>Мова: {currentLanguageObject.nativeName}</Text>
      <Button onPress={cycleLanguage} title="Змінити мову" />
    </View>
  );
};
```

### Використання компонента:
```typescript
import { LanguageSelector } from '@/src/components/LanguageSelector';

<LanguageSelector />
```

## Доступні мови
1. 🇺🇦 Українська (uk) - **за замовчуванням**
2. 🇬🇧 English (en)
3. 🇷🇺 Русский (ru)
4. 🇵🇱 Polski (pl)
