# Як перезавантажити застосунок після змін

## ⚠️ ВАЖЛИВО

Після додавання Toast сповіщень потрібно **перезавантажити застосунок**, щоб зміни застосувались.

## Способи перезавантаження

### Спосіб 1: Через Dev Menu (Рекомендовано)

#### Android:
1. **Потрясіть пристрій** або натисніть `Ctrl+M` (якщо емулятор)
2. У меню виберіть **"Reload"**

#### iOS:
1. **Потрясіть пристрій** або натисніть `Cmd+D` (якщо симулятор)
2. У меню виберіть **"Reload"**

### Спосіб 2: Через Metro Bundler

У терміналі де запущено Metro (термінал 1), натисніть клавішу **`r`**

```bash
 r  - reload app(s)
```

### Спосіб 3: Повне перезавантаження (якщо не допомагає)

#### Android:
```bash
cd /Users/Den/Sites/Voll/React-native/effetex-app
npx react-native run-android
```

#### iOS:
```bash
cd /Users/Den/Sites/Voll/React-native/effetex-app
npx react-native run-ios
```

### Спосіб 4: Закрити та відкрити застосунок заново

Просто закрийте застосунок на пристрої та відкрийте його знову.

## Що було змінено

✅ Додано Toast компонент з кастомною конфігурацією
✅ Замінено всі Alert.alert на toast повідомлення
✅ Додано красиві стилі для повідомлень
✅ Додано переклади українською та російською

## Як перевірити, що Toast працює

Після перезавантаження застосунку:

1. **Спробуйте залогінитись** - побачите зелене toast повідомлення зверху
2. **Додайте товар в обране** - побачите toast з назвою товару
3. **Додайте товар в кошик** - побачите toast з назвою товару

## Якщо все ще показується старий Alert

1. **Очистіть кеш Metro:**
   ```bash
   # У терміналі де працює Metro, натисніть Ctrl+C
   # Потім запустіть:
   npx react-native start --reset-cache
   ```

2. **Видаліть build та перезберіть:**
   ```bash
   cd android
   ./gradlew clean
   cd ..
   npx react-native run-android
   ```

3. **Перезапустіть Metro та застосунок:**
   ```bash
   # Закрийте Metro (Ctrl+C)
   # Видаліть застосунок з пристрою
   # Запустіть заново:
   npx react-native start
   # В іншому терміналі:
   npx react-native run-android
   ```

## Очікуваний результат

Замість такого Alert:
```
┌─────────────────────┐
│      Успішно        │
│ Ви успішно увійшли! │
│                     │
│        [OK]         │
└─────────────────────┘
```

Побачите красивий Toast зверху екрану:
```
┌────────────────────────────┐
│ ✅ Успішно                 │
│ Ви успішно увійшли!        │
└────────────────────────────┘
```

З анімацією появи та автоматичним зникненням через 3 секунди! 🎉

## Налаштування Toast

Можна змінити в `src/utils/toastConfig.ts`:
- Кольори (borderLeftColor)
- Висоту (height)
- Розмір тексту (fontSize)
- Позицію (в `src/utils/toast.ts` - position, topOffset)

