import React, { FC } from 'react';
import { View } from 'react-native';

// Styles
import { styles } from './styles';

export const HiddenTabButton: FC = () => {
  return <View style={styles.hiddenTabButton} />;
};
