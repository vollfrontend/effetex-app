import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 10,
    backgroundColor: '#121212',
  },

  text: {
    color: '#ffffff',
    fontSize: 16,
    fontWeight: '600',
  },
});
