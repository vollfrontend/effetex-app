# Красиві Toast Сповіщення

## Що було зроблено

✅ Додано бібліотеку `react-native-toast-message` для красивих сповіщень

✅ Створено утиліту для зручної роботи з toast повідомленнями

✅ Замінено всі `Alert.alert` на красиві toast повідомлення

✅ Додано сповіщення для всіх ключових дій:
  - Логін / Реєстрація / Вихід
  - Додавання / Видалення товарів з обраного
  - Додавання / Видалення товарів з кошика

## Файли, які були змінені

### 1. App.tsx
Додано компонент Toast в корінь застосунку з кастомною конфігурацією:

```tsx
import Toast from 'react-native-toast-message';
import { toastConfig } from '@/src/utils/toastConfig';

return (
  <>
    <RootNavigation />
    <Toast config={toastConfig} />
  </>
);
```

### 2. src/utils/toast.ts (НОВИЙ ФАЙЛ)
Створено утиліту з helper функціями:

- `showSuccess(title, message)` - успішне повідомлення (зелене)
- `showError(title, message)` - повідомлення про помилку (червоне)
- `showInfo(title, message)` - інформаційне повідомлення (синє)
- `hideToast()` - приховати всі повідомлення

### 3. src/screens/AuthScreen/index.tsx
Замінено `Alert.alert` на toast:

- При успішному логіні
- При успішній реєстрації
- При помилках автентифікації

### 4. src/components/SideMenu/index.tsx
Додано toast при виході з системи:

```tsx
const handleLogout = () => {
  logout();
  handleClose();
  showSuccess(t('auth.success'), t('auth.logoutSuccess'));
  nav('Home');
};
```

### 5. src/state/slices/favoritesSlice.ts
Додано toast при роботі з обраним:

- Успішне додавання товару
- Успішне видалення товару
- Помилки API

### 6. src/state/slices/cartSlice.ts
Додано toast при роботі з кошиком:

- Додавання товару до кошика
- Видалення товару з кошика

### 7. Переклади (ua.json, ru.json)
Додано нові ключі перекладів:

**auth:**
- `logoutSuccess` - "Ви успішно вийшли з системи"

**favorites:**
- `addedToFavorites` - "Товар додано до обраного"
- `removedFromFavorites` - "Товар видалено з обраного"

**cart:**
- `addedToCart` - "Товар додано до кошика"
- `removedFromCart` - "Товар видалено з кошика"

## Як це працює

### Показ повідомлення

```typescript
import { showSuccess, showError, showInfo } from '@/src/utils/toast';

// Успішне повідомлення
showSuccess('Успішно', 'Товар додано до кошика');

// Помилка
showError('Помилка', 'Не вдалося завантажити дані');

// Інформація
showInfo('Інформація', 'Оновлення доступне');
```

### Налаштування

Toast повідомлення автоматично:
- Показуються зверху екрану (position: 'top', topOffset: 60)
- Зникають через 3 секунди (visibilityTime: 3000)
- Мають красиву анімацію появи/зникнення
- Адаптуються під тему застосунку

## Переваги Toast над Alert

✨ **Красивіший UI** - сучасний дизайн замість стандартного Alert

✨ **Не блокують інтерфейс** - користувач може продовжувати роботу

✨ **Автоматично зникають** - не потрібно натискати OK

✨ **Показують назву товару** - контекстна інформація

✨ **Кольорове кодування** - зелений (успіх), червоний (помилка), синій (інфо)

✨ **Плавна анімація** - приємний візуальний ефект

## Приклади використання

### Логін
```
✅ Успішно
Ви успішно увійшли!
```

### Додавання до обраного
```
✅ Товар додано до обраного
iPhone 14 Pro Max
```

### Додавання до кошика
```
✅ Товар додано до кошика
MacBook Pro 16"
```

### Помилка
```
❌ Помилка
Не вдалося завантажити дані
```

### Вихід
```
✅ Успішно
Ви успішно вийшли з системи
```

## Кастомізація

Можна налаштувати:
- Тривалість показу (duration)
- Позицію (top/bottom)
- Відступ зверху (topOffset)
- Стилі через Toast.config()

## Сумісність

- ✅ iOS
- ✅ Android
- ✅ Підтримка RTL
- ✅ Адаптивний дизайн
- ✅ Темна/Світла тема

## Тестування

Щоб протестувати toast повідомлення:

1. Залогіньтесь - побачите зелене повідомлення
2. Додайте товар в обране - побачите повідомлення з назвою товару
3. Додайте товар в кошик - побачите повідомлення
4. Вилогіньтесь - побачите повідомлення про вихід
5. Спробуйте помилковий логін - побачите червоне повідомлення

Всі повідомлення автоматично зникають через 3 секунди! 🎉

