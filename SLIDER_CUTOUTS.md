# Вирізи в слайдері - Реалізація як на веб-сайті

## 📋 Що було реалізовано:

### 1. **Вирізи для кнопок навігації**
Створено заокруглені вирізи з боків слайдера, які імітують CSS псевдоелементи `:before` і `:after` з веб-версії.

### 2. **Структура вирізів**

В React Native немає псевдоелементів, тому вирізи створено за допомогою окремих `View` компонентів:

**Ліворуч:**
- `cutoutLeftTop` - верхній виріз (заокруглення `borderBottomRightRadius`)
- `cutoutLeftBottom` - нижній виріз (заокруглення `borderTopRightRadius`)

**Праворуч:**
- `cutoutRightTop` - верхній виріз (заокруглення `borderBottomLeftRadius`)
- `cutoutRightBottom` - нижній виріз (заокруглення `borderTopLeftRadius`)

### 3. **Параметри вирізів**
```typescript
width: 20px
height: 40px (кожен виріз)
borderRadius: 40px (для створення напівкруглого ефекту)
backgroundColor: динамічний (передається через props)
```

### 4. **Білі кнопки навігації**
```typescript
width: 32px
height: 60px
borderRadius: 8px
backgroundColor: #FFFFFF
```

Кнопки розміщені поверх вирізів (`zIndex: 10`)

## 🎨 Візуальна схема:

```
          Фон сторінки
     ╔════════════════════╗
 ╭───╯                    ╰───╮
 │ ◄  [Фото категорії]     ► │
 ╰───╮                    ╭───╯
     ║  [НАЗВА]           ║
     ╚════════════════════╝
          ● ○ ○ ○ ○
```

## 🔧 Технічні деталі:

### Позиціонування:
```typescript
cutoutLeft/Right: {
  position: 'absolute',
  left/right: 0,
  top: '50%',
  transform: [{ translateY: -40 }], // Центрування
  zIndex: 5,
}

navButton: {
  left/right: -8,  // Виступають за межі слайдера
  zIndex: 10,      // Над вирізами
}
```

### Колір фону вирізів:
Передається через prop `backgroundColor` у компонент `PromoSlider`:
```tsx
<PromoSlider
  data={slidesData}
  onSlidePress={handleSlidePress}
  backgroundColor={theme.background}  // Динамічний колір
/>
```

## 📝 Оновлені файли:

1. **PromoSlider/index.tsx**
   - Додано 4 View для вирізів
   - Додано prop `backgroundColor`
   - Застосовано динамічний колір до вирізів

2. **PromoSlider/styles.ts**
   - Додано стилі для 6 нових елементів:
     - `cutoutLeft`, `cutoutRight`
     - `cutoutLeftTop`, `cutoutLeftBottom`
     - `cutoutRightTop`, `cutoutRightBottom`
   - Оновлено позиціонування кнопок

3. **PromoSlider/types.ts**
   - Додано prop `backgroundColor?: string`

4. **HomeScreen/index.tsx**
   - Передано `backgroundColor={theme.background}` до PromoSlider

## 🎯 Результат:

Слайдер тепер має вирізи з боків, точно як на веб-версії:
- ✅ Заокруглені вирізи зверху і знизу
- ✅ Білі кнопки навігації в центрі вирізів
- ✅ Динамічний колір фону (підтримка тем)
- ✅ Великі заокруглені краї слайдера (24px)
- ✅ Плавна пагінація

## 💡 Переваги реалізації:

1. **Адаптивність**: Колір вирізів автоматично підлаштовується під тему
2. **Чистий код**: Використання окремих View замість складних shape overlay
3. **Легка підтримка**: Всі параметри легко змінити в стилях
4. **Кросплатформність**: Працює однаково на iOS і Android

## 🔍 Порівняння з веб-версією:

| Веб CSS | React Native |
|---------|--------------|
| `:before` / `:after` | Окремі `View` компоненти |
| `border-radius: 50%` | `borderRadius: 40px` |
| `position: absolute` | `position: 'absolute'` ✓ |
| `background-color: white` | `backgroundColor` (динамічний) |
| `z-index: 10` | `zIndex: 10` ✓ |

